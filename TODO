TODO -
    Add functionality so user can accept offer
    Add functionality so user can swap boxes at the end
    Add boxes 23 & 24, along with their functionality
    Make while len(boxes) in main loop end when len < 1 instead of zero
    Clean up & abstract out code
